╔══════════════════════════════════════════════════════════════════════════════╗
║                    🔄 断点续传功能 - 快速参考卡                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

📌 为什么需要？
   处理27万帧数据时，如果中途报错，之前所有工作都白费！
   现在有了断点续传，可以从断点继续运行。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 基本用法

1️⃣ 正常运行（自动保存检查点）
   python auto_cut_dataset.py --llm-provider gpt ...
   
   → 每处理10个任务自动保存检查点到 ./cut_dataset/checkpoints/

2️⃣ 从断点恢复
   python auto_cut_dataset.py --llm-provider gpt ... \
     --resume-from ./cut_dataset/checkpoints/checkpoint_latest.json
   
   → 从上次中断的地方继续运行

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚙️ 新增参数

--checkpoint-interval N    每处理N个任务保存一次检查点（默认：10）
--resume-from PATH         从检查点文件恢复

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📁 检查点文件

./cut_dataset/checkpoints/
  ├── checkpoint_latest.json              ← 最新（用于恢复）
  ├── checkpoint_progress_*_idx*.json    ← 定期保存
  ├── checkpoint_error_*_idx*.json       ← 出错时保存
  └── checkpoint_final.json              ← 完成时保存

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 使用场景

场景1：处理全部数据
  ┌─ 运行 ────────────────────────────────────────────────┐
  │ python auto_cut_dataset.py --llm-provider gpt ...    │
  │ → 进度: 410/4038 ✓                                   │
  │ → 进度: 420/4038 ✓                                   │
  │ → 进度: 430/4038 ❌ TypeError 报错！                  │
  └───────────────────────────────────────────────────────┘
  
  ┌─ 恢复运行 ────────────────────────────────────────────┐
  │ python auto_cut_dataset.py --llm-provider gpt ... \  │
  │   --resume-from ./cut_dataset/checkpoints/\          │
  │                 checkpoint_latest.json               │
  │ → ℹ️  从索引 431 继续处理                             │
  │ → 进度: 431/4038 ✓                                   │
  │ → 进度: 440/4038 ✓                                   │
  └───────────────────────────────────────────────────────┘

场景2：不稳定网络
  python auto_cut_dataset.py --llm-provider gpt ... \
    --checkpoint-interval 5    ← 更频繁保存，更安全

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 监控进度

# 查看当前进度
cat ./cut_dataset/checkpoints/checkpoint_latest.json | grep "progress"

# 输出: "progress": "431/4038"

# 实时监控
watch -n 5 'cat ./cut_dataset/checkpoints/checkpoint_latest.json | grep progress'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ 重要提示

1. 恢复时参数必须一致（--llm-provider, --llm-model等）
2. 检查点只保存任务描述生成进度（不包括夹爪检测和数据集裁剪）
3. 检查点文件会随进度增大（4000个任务约10-20MB）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 详细文档

完整使用指南：CHECKPOINT_GUIDE.md
一般使用指南：USAGE_GUIDE.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 完整示例（处理27万帧）

# 第一次运行
python auto_cut_dataset.py \
  --llm-provider gpt \
  --llm-api-key 5ffef770a5b148c5920b7b16329e30fa \
  --llm-api-base https://gpt.yunstorm.com/ \
  --llm-api-version 2025-01-01-preview \
  --llm-model gpt-4o \
  --checkpoint-interval 10

# 如果中断，恢复运行
python auto_cut_dataset.py \
  --llm-provider gpt \
  --llm-api-key 5ffef770a5b148c5920b7b16329e30fa \
  --llm-api-base https://gpt.yunstorm.com/ \
  --llm-api-version 2025-01-01-preview \
  --llm-model gpt-4o \
  --resume-from ./cut_dataset/checkpoints/checkpoint_latest.json

╚══════════════════════════════════════════════════════════════════════════════╝
