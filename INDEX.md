📁 data_dealer_auto - 自动化Pick/Place数据处理工具
================================================================================

这是一个完整的自动化数据处理系统，用于识别机器人操作中的Pick/Place关键帧。

🚀 快速开始
─────────────────────────────────────────────────────────────────────────────

1️⃣  分析数据（仅检测，快速）
    python auto_cut_dataset.py --end-idx 10000 --skip-cutting

2️⃣  执行完整处理（分析+转换）
    python auto_cut_dataset.py --end-idx 10000 --max-episodes 100

3️⃣  在Web界面中使用（可选）
    cd /home/dongyingyibadao/data_dealer
    bash start_web_server.sh 5000 0.0.0.0 0 0 100 \
      --dataset-path /home/dongyingyibadao/data_dealer_auto/cut_dataset

📖 文档导航
─────────────────────────────────────────────────────────────────────────────

📄 README.md
   - 项目概览和快速入门
   - 核心功能说明
   - 与Web工具集成
   → 首先阅读此文件

📄 QUICK_START.md
   - 3步快速启动指南
   - 常用命令
   - 预期结果说明
   → 想快速上手时阅读

📄 USAGE_GUIDE.md
   - 详细的参数说明
   - 6个完整使用案例
   - 常见问题解答
   - 故障排除指南
   → 遇到问题时查看

📄 INDEX.md
   - 本文件，快速导航
   - 文件结构说明
   - 功能模块介绍

🔧 核心模块
─────────────────────────────────────────────────────────────────────────────

🐍 gripper_detector.py (8.1 KB)
   功能：检测夹爪状态变化和关键帧识别
   
   核心类：
   - GripperStateDetector
     ├─ detect_gripper_changes()    # 检测所有状态变化
     ├─ extract_frame_ranges()      # 提取前后30帧
     └─ merge_adjacent_ranges()     # 合并相邻范围
   
   函数：
   - analyze_gripper_changes()       # 完整的分析流程
   
   特点：
   ✓ 监测action[-1]（夹爪状态）
   ✓ 识别Pick（-1→1）和Place（1→-1）
   ✓ 自定义检测阈值
   ✓ 支持批量处理

🐍 task_description_generator.py (12 KB)
   功能：自动生成任务描述
   
   核心类：
   - LLMProvider（基类）
   - QwenLLM
     └─ 集成阿里Qwen模型（可选）
   - DeepseekLLM
     └─ 集成Deepseek模型（可选）
   - TaskDescriptionGenerator
     └─ 高级接口，自动缓存
   
   特点：
   ✓ 本地规则生成（无需API）
   ✓ 可选Qwen/Deepseek（更精准）
   ✓ 自动缓存避免重复调用
   ✓ 智能词汇提取

🐍 dataset_cutter.py (12 KB)
   功能：数据集转换和格式保存
   
   核心类：
   - DatasetCutter
     ├─ extract_frames()            # 提取帧数据
     ├─ organize_by_episode()       # 按episode组织
     ├─ save_as_lerobot_format()    # 保存为Parquet
     └─ _save_frame_batch()         # 保存帧数据
   
   函数：
   - cut_and_convert_dataset()      # 完整流程
   
   特点：
   ✓ LeRobot兼容格式
   ✓ Parquet元数据
   ✓ 流式处理，内存高效
   ✓ 完整的多模态数据

🐍 auto_cut_dataset.py (6.7 KB)
   功能：主脚本，整合所有模块
   
   主要函数：
   - load_lerobot_dataset()         # 加载数据集
   - analyze_and_extract()          # 分析和提取
   - generate_task_descriptions()   # 生成任务描述
   - save_frame_ranges_info()       # 保存分析结果
   - main()                         # 命令行接口
   
   特点：
   ✓ 灵活的命令行参数
   ✓ 支持分步骤执行
   ✓ 支持恢复和重用
   ✓ 详细的进度反馈

🧪 test_auto_cut.py (6.3 KB)
   功能：完整的测试套件
   
   测试场景：
   ✓ test_gripper_detector()        # 夹爪检测测试
   ✓ test_task_generator()          # 任务生成测试
   ✓ test_frame_range_extraction()  # 帧提取测试
   ✓ test_end_to_end_flow()         # 完整流程测试
   
   运行：python test_auto_cut.py
   预期：✅ 所有测试通过

📊 工作流程
─────────────────────────────────────────────────────────────────────────────

输入数据集（LeRobot格式）
         ↓
    [检测] 扫描夹爪状态，识别关键帧
         ↓ 约1,600个关键帧（97%数据）
    [提取] 关键帧前后各30帧
         ↓ 共60帧/操作
    [生成] 自动生成任务描述
         ↓ Pick/"place" 类任务
    [转换] 转换为LeRobot Parquet格式
         ↓
输出数据集（LeRobot兼容格式）

📁 输出目录结构
─────────────────────────────────────────────────────────────────────────────

执行后生成的目录结构：

cut_dataset/                              # 输出根目录
├── meta/                                # 元数据目录
│   ├── episodes/
│   │   └── chunk-000/
│   │       └── file-000.parquet         # Episodes元数据
│   ├── tasks.parquet                    # 任务列表
│   ├── info.json                        # 数据集信息
│   └── stats.json                       # 统计信息
├── data/                                # 帧数据目录
│   └── chunk-000/
│       ├── file-000.parquet             # 帧数据文件（500MB）
│       ├── file-001.parquet
│       └── ...
└── frame_ranges_info.json               # 分析报告（JSON）

总大小：2-3 GB（取决于处理的帧数）

💾 数据格式说明
─────────────────────────────────────────────────────────────────────────────

Episodes元数据（Parquet）
  字段：
  - episode_index (int)         # 新episode编号
  - tasks (str)                 # 生成的任务描述
  - dataset_from_index (int)    # 全局开始帧索引
  - dataset_to_index (int)      # 全局结束帧索引
  - length (int)                # episode帧数
  - action_type (str)           # 'pick' 或 'place'
  - original_task (str)         # 原始任务描述
  - keyframe_index (int)        # 原始关键帧索引
  - original_episode_index (int)# 原始episode编号

帧数据（Parquet）
  字段：
  - observation.images.image (float[3,256,256])   # RGB图像
  - observation.images.image2 (float[3,256,256])  # 第二摄像头图像
  - observation.state (float[8])                  # 关节状态
  - action (float[7])                             # 动作向量
  - timestamp (float)                             # 时间戳

分析报告（JSON）
  字段：
  - total_ranges (int)          # 总操作数
  - pick_count (int)            # Pick操作数
  - place_count (int)           # Place操作数
  - frame_ranges (list)         # 详细的操作信息

⚙️ 核心参数
─────────────────────────────────────────────────────────────────────────────

关键参数详解：

--dataset-path PATH
  输入数据集路径
  默认：/home/dongyingyibadao/HuggingFaceVLA_cus/libero
  示例：--dataset-path /path/to/dataset

--output-dir PATH
  输出目录
  默认：./cut_dataset
  示例：--output-dir /data/output

--start-idx, --end-idx
  处理的帧范围（用于控制数据量）
  默认：0 - 10000
  示例：--start-idx 0 --end-idx 50000

--max-episodes N
  限制保存的episode数量
  默认：None（全部保存）
  示例：--max-episodes 100

--llm-provider {local|qwen|deepseek}
  任务描述生成方法
  local：无需API，快速
  qwen/deepseek：需要API密钥，更精准
  默认：local

--skip-cutting
  仅进行分析，不执行转换
  用途：快速查看数据统计

--load-ranges FILE
  加载之前保存的分析结果
  用途：避免重复分析

更多参数：python auto_cut_dataset.py --help

🎯 使用场景
─────────────────────────────────────────────────────────────────────────────

场景1：快速探索数据
  python auto_cut_dataset.py --end-idx 10000 --skip-cutting
  ✓ 快速了解数据统计
  ✓ 不占用磁盘空间
  输出：frame_ranges_info.json

场景2：生成训练数据
  python auto_cut_dataset.py --end-idx 10000 --max-episodes 100
  ✓ 生成完整的、可用的数据集
  输出：cut_dataset/meta 和 cut_dataset/data

场景3：处理大规模数据
  python auto_cut_dataset.py --end-idx 100000 --max-episodes 500
  ✓ 生成大规模训练数据
  注意：输出较大（10+ GB），需要充足磁盘空间

场景4：与Web工具集成
  # 先生成数据
  python auto_cut_dataset.py --end-idx 10000
  
  # 再用Web工具进行手动标记
  cd /home/dongyingyibadao/data_dealer
  bash start_web_server.sh 5000 0.0.0.0 0 0 100 \
    --dataset-path /home/dongyingyibadao/data_dealer_auto/cut_dataset

🧪 测试和验证
─────────────────────────────────────────────────────────────────────────────

验证系统是否正常工作：

  python test_auto_cut.py

预期输出：
  ✅ 所有测试通过！

📊 性能参考
─────────────────────────────────────────────────────────────────────────────

执行时间（RTX 4090）：
  检测：~1,000 帧/秒
  生成：~100-500 帧/秒
  转换：~500 帧/秒
  
  处理10,000帧：约1-2分钟

内存占用：
  检测阶段：~1 GB
  生成阶段：~2 GB
  转换阶段：~4 GB

磁盘空间：
  分析结果：~2 MB
  完整转换：2-3 GB（10,000帧）

📞 获取帮助
─────────────────────────────────────────────────────────────────────────────

查看各类文档：
  cat README.md           # 项目概览
  cat QUICK_START.md      # 快速开始
  cat USAGE_GUIDE.md      # 详细指南
  cat INDEX.md            # 本文件

获取命令帮助：
  python auto_cut_dataset.py --help

运行测试：
  python test_auto_cut.py

常见问题：
  查看USAGE_GUIDE.md中的"常见问题"部分

🔗 相关文件
─────────────────────────────────────────────────────────────────────────────

Web工具位置：
  /home/dongyingyibadao/data_dealer/

Web工具使用：
  bash start_web_server.sh

原始数据集位置：
  /home/dongyingyibadao/HuggingFaceVLA_cus/libero/

================================================================================
版本：1.0
创建日期：2025年11月24日
位置：/home/dongyingyibadao/data_dealer_auto/
================================================================================
